{
  "name": "hastycam",
  "private": true,
  "scripts": {
    "dev": "lerna run dev --stream",
    "build": "lerna bootstrap && lerna run build --stream",
    "test": "lerna run test --stream"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
