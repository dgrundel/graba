{
  "name": "hastycam",
  "private": true,
  "scripts": {
    "start": "lerna run start --stream",
    "build": "lerna run build --stream && lerna bootstrap",
    "test": "lerna run test --stream"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
