{
  "name": "hastycam",
  "private": true,
  "scripts": {
    "start": "lerna run start --stream",
    "build": "lerna bootstrap && lerna run build --stream",
    "test": "lerna run test --stream"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
